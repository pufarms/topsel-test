# 작업 지시서: 대시보드 날짜 필터 위치 및 적용 범위 수정

## 📋 문제점
현재 관리자 대시보드(`/admin`)와 회원 대시보드(`/dashboard`)에서 `DateRangeFilter`가 **대시보드 전체 최상단**에 배치되어 있습니다. 이로 인해 날짜 필터가 회원현황, 매출현황, 문의현황 등 모든 섹션에 적용되는 것처럼 보입니다.

이 날짜 필터는 **주문/배송 현황 섹션에만 해당**하는 필터이므로, 위치와 적용 범위를 정확히 수정해야 합니다.

---

## 🎯 수정 내용

### 1. 관리자 대시보드 (`/admin`)

**현재 (잘못된 상태):**
```
[기간: 오늘 | 어제 | 이번 주 | 이번 달 | 직접 선택]  ← 대시보드 최상단에 위치
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 회원 현황  (날짜 필터와 무관한 섹션)
📈 매출 현황  (날짜 필터와 무관한 섹션)
📦 주문/배송 현황  (이 섹션에만 적용되어야 함)
💬 문의 현황  (날짜 필터와 무관한 섹션)
🎪 행사진행 현황  (날짜 필터와 무관한 섹션)
```

**수정 후 (올바른 상태):**
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 회원 현황  (날짜 필터 없음 - 항상 전체 현황 표시)
📈 매출 현황  (날짜 필터 없음 - 자체 기간 표시: 금일/전일/전월/이번달)
📦 주문/배송 현황
   [기간: 오늘 | 어제 | 이번 주 | 이번 달 | 직접 선택]  ← 이 섹션 내부에 위치
   전체주문 0건 | 주문대기 0건 | 주문조정 0건 | 상품준비중 0건 | 배송준비중 0건 | 회원취소 0건 | 배송중 0건
💬 문의 현황  (날짜 필터 없음)
🎪 행사진행 현황  (날짜 필터 없음)
```

**구체적 수정:**
- `DateRangeFilter`를 대시보드 최상단에서 제거
- "📦 주문/배송 현황" 섹션 제목 바로 옆 또는 바로 아래에 `DateRangeFilter` 배치
- 레이아웃 예시: 섹션 헤더 행에 왼쪽은 "📦 주문/배송 현황" 제목, 오른쪽에 날짜 필터 배치 (한 줄로 정리)
- 날짜 필터 변경 시 주문/배송 현황 카드의 수치만 갱신 (다른 섹션에 영향 없음)

---

### 2. 회원 대시보드 (`/dashboard`)

**현재 (잘못된 상태):**
```
마이페이지 대시보드
[기간: 오늘 | 어제 | 이번 주 | 이번 달 | 직접 선택]  ← 최상단에 위치
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
회원등급 / 지난달 매입총액 / 이번달 매입총액
🛒 주문현황  (이 섹션에만 적용되어야 함)
💰 현재 예치금, 포인터 현황  (날짜 필터와 무관)
📢 공지사항 / 문의게시판  (날짜 필터와 무관)
```

**수정 후 (올바른 상태):**
```
마이페이지 대시보드
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
회원등급 / 지난달 매입총액 / 이번달 매입총액  (날짜 필터 없음)
🛒 주문현황
   [기간: 오늘 | 어제 | 이번 주 | 이번 달 | 직접 선택]  ← 이 섹션 내부에 위치
   전체주문 0건 | 주문대기 0건 | 주문조정 0건 | 상품준비중 0건 | 배송준비중 0건 | 회원취소 0건 | 배송중 0건
   [+ 신규주문 등록] [취소 리스트확인] [송장파일 다운] [취소건 등록] [배송중 조회]  ← 퀵 액션 버튼
💰 현재 예치금, 포인터 현황  (날짜 필터 없음)
📢 공지사항 / 문의게시판  (날짜 필터 없음)
```

**구체적 수정:**
- `DateRangeFilter`를 대시보드 최상단에서 제거
- "🛒 주문현황" 섹션 제목 바로 옆 또는 바로 아래에 `DateRangeFilter` 배치
- 레이아웃 예시: 섹션 헤더 행에 왼쪽은 "🛒 주문현황" 제목, 오른쪽에 날짜 필터 배치
- 날짜 필터 변경 시 주문현황 카드의 수치만 갱신 (다른 섹션에 영향 없음)

---

## ⚠️ 주의사항

1. **주문관리 8개 페이지와 회원 주문 탭의 날짜 필터는 현재 위치 그대로 유지** — 이번 수정은 대시보드(관리자/회원)만 해당
2. 다른 섹션(회원현황, 매출현황, 예치금, 공지사항 등)은 날짜 필터의 영향을 받지 않아야 함
3. 날짜 필터의 디자인/스타일은 기존과 동일하게 유지하되, 크기가 섹션 내부에 맞게 조정될 수 있음

## ✅ 완료 기준
- [ ] 관리자 대시보드: 날짜 필터가 "주문/배송 현황" 섹션 내부에만 위치
- [ ] 회원 대시보드: 날짜 필터가 "주문현황" 섹션 내부에만 위치
- [ ] 날짜 변경 시 주문 관련 수치만 변경되고 다른 섹션은 영향 없음
- [ ] 대시보드 최상단에 날짜 필터가 더 이상 표시되지 않음
