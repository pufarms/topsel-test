# 회계장부 시스템 요구사항 (수정본)

> 관리자 대시보드에 **[회계장부]** 메뉴를 추가합니다.
> 원재료(원물/반재료/부자재/기타비용) 매입을 관리하고, 공급업체별 외상 정산을 추적합니다.
> 매출은 사이트 주문 자동 연동 + 납품 건 수기 입력으로 관리합니다.
> 이후 AI 통합 분석(매출+매입=수익성)의 데이터 기반이 됩니다.

---

## 1. 사이드바 메뉴 위치

```
관리자 대시보드
├─ 📊 대시보드
├─ 👥 회원관리
├─ 🛒 주문관리
├─ 📦 상품관리
├─ 🏬 재고관리
├─ 🏦 입금관리
├─ 📈 통계관리
├─ 📒 회계장부         ← 기존 '정산관리' 메뉴를 '회계장부'로 변경
│   ├─ [회원 정산]     ← 기존 정산관리 기능 (포인터/예치금 정산)
│   ├─ [공급업체 관리]  ← 신규 (기존 외주업체 연동)
│   ├─ [매입 관리]     ← 신규 (사이트 자동 + 수기)
│   ├─ [매입 정산]     ← 신규 (업체별 외상 관리)
│   ├─ [매출 현황]     ← 신규 (사이트 자동 + 납품 수기 + 세금계산서)
│   └─ [손익 현황]     ← 신규 (매출 - 매입 = 이익)
├─ 🎟️ 쿠폰관리
├─ 💬 카카오 알림
├─ 📄 페이지관리
└─ ⚙️ 설정
```

> **변경 내용:**
> - 사이드바에서 "💰 정산관리" 메뉴를 "📒 회계장부"로 **이름 변경 + 탭 확장**
> - 기존 정산관리 기능(회원 포인터/예치금 정산)은 **[회원 정산] 탭**으로 그대로 유지
> - 회계장부 신규 탭 5개 추가 (공급업체, 매입, 매입정산, 매출, 손익)

---

## 2. 탭 구조

```
회계장부
├─ [회원 정산]       ← 기존 정산관리 기능 그대로 유지 (포인터/예치금 정산 이력)
├─ [공급업체 관리]   ← 기존 외주업체 연동 + 회계 추가 정보 + 외상 잔액 확인
├─ [매입 관리]       ← 사이트 외주거래(자동) + 직접 매입(수기/엑셀)
├─ [매입 정산]       ← 공급업체별 입금(대금 지급) 기록 + 외상 잔액 관리
├─ [매출 현황]       ← 사이트 주문(자동) + 납품(수기) + 세금계산서(포인터 제외)
└─ [손익 현황]       ← 매출 - 매입 = 이익 (자동 계산)
```

> **[회원 정산] 탭**: 기존 정산관리 페이지의 UI와 기능을 그대로 가져옵니다.
> 별도 개발 없이 기존 컴포넌트를 회계장부 탭 안에 배치합니다.

---

## 3. 공급업체 관리 탭

### 3-1. 개요

> **기존 사이트의 외주업체(vendors 테이블)를 그대로 활용합니다.**
> 별도 공급업체 테이블을 새로 만들지 않습니다.
> 기존 vendors 테이블에 회계 관련 필드만 추가합니다.

```
사이트 외주업체 관리에서 업체 등록/수정
       ↓ 자동 연동
회계장부 공급업체 관리에서 조회 + 회계 정보 관리 (외상, 정산)
```

### 3-2. 기존 vendors 테이블 확장 (필드 추가만)

기존 vendors 테이블에 아래 필드를 **추가**합니다. 기존 필드는 수정하지 않습니다.

```sql
-- 기존 vendors 테이블에 회계 관련 필드 추가
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS supply_type TEXT[] DEFAULT '{}';
  -- 공급 유형: ['raw', 'semi', 'subsidiary', 'etc']
  -- raw=원물, semi=반재료, subsidiary=부자재, etc=기타(물류비 등)

ALTER TABLE vendors ADD COLUMN IF NOT EXISTS business_number VARCHAR(20);
  -- 사업자번호 (세금계산서 발행용)

ALTER TABLE vendors ADD COLUMN IF NOT EXISTS address TEXT;
  -- 사업장 주소
```

> 기존 필드(name, contactName, phone, email, bankName, accountNumber, accountHolder, settlementCycle, loginId, loginPassword, isActive, memo 등)는 그대로 활용합니다.

### 3-3. 공급업체 목록 화면

```
┌──────────────────────────────────────────────────────────────┐
│  공급업체 관리                                                  │
│                                                                │
│  ℹ️ 사이트 외주업체 관리와 자동 연동됩니다.                       │
│     업체 등록/수정은 [외주업체 관리] 메뉴에서 합니다.              │
│                                                                │
│  🔍 업체명 검색...    공급유형: [전체 ▼]    상태: [전체 ▼]       │
│                                                                │
│  ┌────────────────────────────────────────────────────────────┐│
│  │ 업체명     담당자  연락처        공급유형    외상잔액         ││
│  ├────────────────────────────────────────────────────────────┤│
│  │ 행복농장   김철수  010-1234-5678  원물      1,230,000  [설정]││
│  │ 제주감귤원  박영희  010-2345-6789  원물        560,000  [설정]││
│  │ 딸기마을   이정수  010-3456-7890  원물        890,000  [설정]││
│  │ 프루츠팩   최미영  010-4567-8901  부자재      320,000  [설정]││
│  │ CJ대한통운  -     1588-1255     기타        180,000  [설정]││
│  └────────────────────────────────────────────────────────────┘│
│                                                                │
│  총 외상 잔액 합계: 3,180,000원                                  │
└──────────────────────────────────────────────────────────────┘
```

- **업체 기본 정보** (업체명, 담당자, 연락처, 계좌 등): 기존 외주업체 관리에서 연동 (읽기 전용)
- **회계 정보** (공급 유형, 사업자번호): 회계장부에서 [설정] 클릭하여 추가 입력
- **외상 잔액**: 매입 합계 - 입금 합계 자동 계산
- 외상 잔액 클릭 시 → [매입 정산] 탭 해당 업체로 이동

### 3-4. 회계 설정 모달 ([설정] 클릭 시)

기존 업체 정보를 보여주면서, 회계에 필요한 추가 정보만 입력합니다.

```
┌──────────────────────────────────────────┐
│  행복농장 — 회계 설정                       │
│                                            │
│  ── 기본 정보 (외주업체 관리 연동) ──        │
│  업체명: 행복농장                           │
│  담당자: 김철수  |  연락처: 010-1234-5678   │
│  은행: 농협  |  계좌: 123-4567-8901-23     │
│  ℹ️ 수정은 [외주업체 관리]에서 합니다        │
│                                            │
│  ── 회계 추가 정보 ──                       │
│  공급 유형: [✅원물 □반재료 □부자재 □기타]   │
│  사업자번호: [123-45-67890          ]      │
│  주소:      [경북 안동시 ...          ]     │
│                                            │
│              [취소]  [저장]                  │
└──────────────────────────────────────────┘
```

### 3-5. API

| 엔드포인트 | Method | 설명 |
|-----------|--------|------|
| GET /api/admin/accounting/vendors | GET | 공급업체 목록 (기존 vendors + 외상잔액 + 회계정보) |
| GET /api/admin/accounting/vendors/:id | GET | 업체 상세 (기본정보 + 회계정보 + 최근 거래내역) |
| PUT /api/admin/accounting/vendors/:id | PUT | 회계 추가 정보만 수정 (supply_type, business_number, address) |

---

## 4. 매입 관리 탭

### 4-1. 두 가지 매입 소스 (매출 현황과 동일한 구조)

> 매출 현황이 **사이트 매출(자동) + 직접 매출(수기)** 인 것처럼,
> 매입 관리도 **사이트 외주거래(자동) + 직접 매입(수기)** 으로 구성합니다.

```
매출 현황:  사이트 주문 → 자동 연동  +  납품 건 → 수기 입력
매입 관리:  사이트 외주거래 → 자동 연동  +  직접 매입 → 수기 입력
```

1. **사이트 매입 (자동 연동)**
   - 사이트에서 외주업체에 배분 → 출고 확정된 건
   - 기존 데이터: pending_orders의 외주 배분 건 (vendorId + vendorPrice)
   - 매입 확정 시점: 주문 status = 'shipping' 이후 (매출 확정과 동일 시점)
   - 매입액 = product_vendors의 vendorPrice × 수량

2. **직접 매입 (수기 입력)**
   - 사이트 거래 외의 원물/반재료/부자재/기타 매입
   - 엑셀 스타일 인라인 입력 또는 엑셀 업로드

### 4-2. 원재료 분류 체계

| 분류 | 코드 | 설명 | 예시 |
|------|------|------|------|
| 원물 | raw | 농산물 원재료 | 사과 원물, 배, 감귤, 딸기 |
| 반재료 | semi | 가공/세척/선별된 것 | 세척 사과, 선별 감귤, 탈피 키위 |
| 부자재 | subsidiary | 포장/용기 등 소모품 | 포장박스, 완충재, 스티커, 테이프, 아이스팩 |
| 기타 | etc | 물류비, 운송비 등 비용 | 택배비, 화물 운송비, 냉장 보관료 |

### 4-3. 매입 목록 화면

```
┌──────────────────────────────────────────────────────────────────┐
│  매입 관리                                                        │
│                                                                    │
│  기간: [2026-02-01] ~ [2026-02-11]                                │
│  구분: [전체 ▼] (전체 / 사이트 매입 / 직접 매입)                    │
│  분류: [전체 ▼]  업체: [전체 ▼]  품목 검색: [          ]            │
│                                                                    │
│  모드: [조회] [✏️ 입력]  [📤 엑셀 업로드] [📋 템플릿] [📥 다운로드]  │
│                                                                    │
│  ┌─ 매입 요약 ────────────────────────────────────────────────┐    │
│  │  총 매입: 12,534,000원                                      │    │
│  │  ├─ 사이트 매입: 8,456,000원 (127건) — 자동 연동             │    │
│  │  └─ 직접 매입:  4,078,000원 (42건)  — 수기 입력              │    │
│  └──────────────────────────────────────────────────────────┘    │
│                                                                    │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │ □  날짜   구분   분류   업체명    품목명     수량  단가   금액 │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │    2/11  🔗사이트 원물  행복농장  사과10kg   50   14,000  700K│    │
│  │    2/11  🔗사이트 원물  제주감귤  감귤5kg    30   12,000  360K│    │
│  │ □  2/11  ✏️직접  원물  딸기마을  딸기1kg    40   18,000  720K│    │
│  │ □  2/11  ✏️직접  부자재 프루츠팩  포장박스   200  1,500   300K│    │
│  │ □  2/11  ✏️직접  기타  CJ대한통운 택배비     1   180,000 180K│    │
│  │    2/10  🔗사이트 원물  행복농장  배7.5kg   25   20,000  500K│    │
│  │ ...                                                        │    │
│  ├────────────────────────────────────────────────────────────┤    │
│  │  선택: [일괄 삭제]  (직접 매입만 삭제 가능)  합계: 12,534,000원│    │
│  └────────────────────────────────────────────────────────────┘    │
│                                                                    │
│  ┌─ 분류별 요약 ──────────────────────────────────────────────┐    │
│  │  원물: 9,680,000원 (77%)  |  반재료: 450,000원 (4%)        │    │
│  │  부자재: 1,284,000원 (10%) |  기타: 1,120,000원 (9%)       │    │
│  └──────────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
```

- **🔗사이트**: 사이트 외주거래에서 자동 연동된 건 (수정/삭제 불가, 읽기 전용)
- **✏️직접**: 수기 입력한 건 (수정/삭제 가능)
- 사이트 매입은 체크박스 없음 (삭제 불가)
- 직접 매입만 체크박스 선택 → 일괄 삭제 가능

### 4-4. 사이트 매입 자동 연동 로직

```
사이트 주문 확정 (status → shipping)
    ↓
해당 주문에 외주 배분 건이 있는지 확인
(pending_orders에서 vendorId가 있는 건)
    ↓
있으면 → 회계장부 매입으로 자동 기록
    - 업체: vendorId → vendors 테이블
    - 품목: 해당 상품명
    - 수량: 배분 수량
    - 단가: product_vendors의 vendorPrice (매입가)
    - 분류: 해당 업체의 supply_type (기본: 원물)
    - 구분: 'site' (사이트 매입)
```

> 매출 현황에서 orders 테이블의 확정 주문을 자동 집계하는 것과 같은 방식입니다.
> 별도 테이블에 복사하지 않고, 조회 시점에 기존 데이터를 집계합니다.

### 4-3. 매입 등록 모달 (수기 입력)

```
┌──────────────────────────────────────────────────────┐
│  매입 등록                                             │
│                                                        │
│  매입일: [2026-02-11]                                  │
│  공급업체: [행복농장 ▼]                                 │
│                                                        │
│  ┌─ 품목 목록 ───────────────────────────────────────┐ │
│  │ #  분류    품목명      수량  단위   단가     금액    │ │
│  │ 1  [원물▼]  사과 10kg  50   [박스▼] 14,000  700,000 │ │
│  │ 2  [원물▼]  배 7.5kg   25   [박스▼] 20,000  500,000 │ │
│  │                                                    │ │
│  │              [+ 품목 추가]     [행 삭제]             │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  합계: 1,200,000원                                     │
│  메모: [                                   ]           │
│                                                        │
│                          [취소]  [등록]                  │
└──────────────────────────────────────────────────────┘
```

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 매입일 | date | ✅ | 매입 날짜 (기본값: 오늘) |
| 공급업체 | select | ✅ | 등록된 업체 목록에서 선택 |
| 분류 | select | ✅ | 원물 / 반재료 / 부자재 / 기타 |
| 품목명 | text | ✅ | 구체적 품목명 (예: 사과 10kg, 포장박스 10호) |
| 수량 | number | ✅ | 매입 수량 |
| 단위 | select | ✅ | 박스 / kg / 팩 / 송이 / 개 / 롤 / 건 |
| 단가 | number | ✅ | 개당 매입 단가 (원) |
| 금액 | auto | | 수량 × 단가 자동 계산 |
| 메모 | text | | 비고 |

- 여러 품목 동시 등록: [+ 품목 추가] 버튼으로 행 추가
- 등록 시 해당 업체 외상 잔액 자동 증가

### 4-4. 엑셀 업로드

#### 템플릿 구조

| 매입일 | 업체명 | 분류 | 품목명 | 수량 | 단위 | 단가 | 메모 |
|--------|--------|------|--------|------|------|------|------|
| 2026-02-11 | 행복농장 | 원물 | 사과 10kg | 50 | 박스 | 14000 | |
| 2026-02-11 | 프루츠팩 | 부자재 | 포장박스 10호 | 200 | 개 | 1500 | |
| 2026-02-11 | CJ대한통운 | 기타 | 택배비 | 1 | 건 | 180000 | 2월 1~10일분 |

- **분류** 열: "원물", "반재료", "부자재", "기타" 중 하나
- **업체명**: 등록된 업체명과 매칭 (미등록 시 오류 표시)

#### 업로드 프로세스
1. [📤 엑셀 업로드] 클릭 → 파일 선택 (xlsx, xls, csv)
2. 데이터 미리보기 + 유효성 검증
3. 오류 행 빨간 하이라이트 + 오류 메시지
4. [확인] → 정상 행만 일괄 등록
5. 결과: "총 42건 중 40건 등록 성공, 2건 오류"

### 4-5. DB 테이블: purchases

```sql
CREATE TABLE purchases (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  purchase_date DATE NOT NULL,
  vendor_id UUID NOT NULL REFERENCES vendors(id),
  material_type VARCHAR(20) NOT NULL,      -- raw, semi, subsidiary, etc
  product_name VARCHAR(200) NOT NULL,
  quantity DECIMAL(10,2) NOT NULL,
  unit VARCHAR(20) NOT NULL,
  unit_price DECIMAL(12,0) NOT NULL,
  total_amount DECIMAL(14,0) NOT NULL,
  memo TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_purchases_date ON purchases(purchase_date);
CREATE INDEX idx_purchases_vendor ON purchases(vendor_id);
CREATE INDEX idx_purchases_type ON purchases(material_type);
```

### 4-6. API

| 엔드포인트 | Method | 설명 |
|-----------|--------|------|
| GET /api/admin/purchases | GET | 매입 목록 (기간, 업체, 분류 필터 + 페이지네이션) |
| GET /api/admin/purchases/summary | GET | 기간 매입 요약 (분류별 합계) |
| POST /api/admin/purchases | POST | 매입 등록 (여러 품목 동시) |
| PUT /api/admin/purchases/:id | PUT | 매입 수정 |
| DELETE /api/admin/purchases/:id | DELETE | 매입 삭제 |
| POST /api/admin/purchases/batch-delete | POST | 매입 일괄 삭제 |
| POST /api/admin/purchases/upload | POST | 엑셀 업로드 |
| GET /api/admin/purchases/template | GET | 엑셀 템플릿 다운로드 |
| GET /api/admin/purchases/export | GET | 매입 목록 엑셀 다운로드 |

---

## 5. 매입 정산 탭

### 5-1. 개요

공급업체별 **외상 잔액**(미지급금)을 관리합니다.

```
외상 잔액 = 누적 매입 총액 - 누적 입금 총액

매입 등록 → 외상 잔액 자동 증가 (+)
입금 등록 → 외상 잔액 자동 감소 (-)
```

### 5-2. 정산 화면

```
┌──────────────────────────────────────────────────────────────┐
│  매입 정산                                                     │
│                                                                │
│  ┌─ 업체별 외상 현황 ────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  업체명      총 매입액     총 입금액     외상 잔액    상태   │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │  행복농장    12,400,000   11,170,000   1,230,000   ⚠️     │  │
│  │  딸기마을     8,900,000    8,010,000     890,000   ⚠️     │  │
│  │  제주감귤원   6,800,000    6,240,000     560,000          │  │
│  │  프루츠팩     3,200,000    2,880,000     320,000          │  │
│  │  CJ대한통운   1,800,000    1,620,000     180,000          │  │
│  ├────────────────────────────────────────────────────────────┤  │
│  │  합계       33,100,000   29,920,000   3,180,000          │  │
│  └────────────────────────────────────────────────────────────┘  │
│                                                                │
│  ⚠️ = 외상 잔액 100만원 이상                                    │
│                                                                │
│  ── 업체 클릭 시 아래 상세 표시 ──                                │
│                                                                │
│  ┌─ 행복농장 거래 내역 ──────────────────────────────────────┐  │
│  │                                                            │  │
│  │  기간: [2026-01-01] ~ [2026-02-11]    [+ 입금 등록]        │  │
│  │                                                            │  │
│  │  날짜     구분      내용           금액       잔액          │  │
│  │  2/11    매입      사과10kg 50박스  +700,000  1,230,000    │  │
│  │  2/10    매입      배7.5kg 25박스   +500,000    530,000    │  │
│  │  2/9     입금      계좌이체         -800,000     30,000    │  │
│  │  2/8     매입      사과10kg 45박스  +630,000    830,000    │  │
│  │  2/7     매입      배7.5kg 20박스   +400,000    200,000    │  │
│  │  2/5     입금      계좌이체         -500,000   -200,000    │  │
│  │  ...                                                      │  │
│  │                                                            │  │
│  │  [📥 거래내역 다운로드 (엑셀)]                               │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

### 5-3. 입금 등록 모달

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 공급업체 | auto | ✅ | 선택된 업체 (자동) |
| 입금일 | date | ✅ | 입금 날짜 (기본값: 오늘) |
| 입금액 | number | ✅ | 입금 금액 (원) |
| 메모 | text | | 비고 (정산 기간, 계좌이체 등) |

### 5-4. DB 테이블: vendor_payments

```sql
CREATE TABLE vendor_payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  vendor_id UUID NOT NULL REFERENCES vendors(id),
  payment_date DATE NOT NULL,
  amount DECIMAL(14,0) NOT NULL,
  memo TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_vendor_payments_vendor ON vendor_payments(vendor_id);
CREATE INDEX idx_vendor_payments_date ON vendor_payments(payment_date);
```

### 5-5. 외상 잔액 계산 (실시간, 별도 필드 없음)

외상 잔액 = **(직접 매입 + 사이트 매입)** - 입금 합계

```sql
-- 업체별 외상 잔액 (직접 매입 + 사이트 매입 통합)
WITH direct_purchases AS (
  -- 직접 매입 (purchases 테이블)
  SELECT vendor_id, SUM(total_amount) AS total
  FROM purchases GROUP BY vendor_id
),
site_purchases AS (
  -- 사이트 매입 (외주 배분 확정 건)
  SELECT po."vendorId" AS vendor_id,
         SUM(pv."vendorPrice" * po.quantity) AS total
  FROM pending_orders po
  JOIN product_vendors pv ON pv."vendorId" = po."vendorId"
                         AND pv."productCode" = po."productCode"
  JOIN orders o ON o.id = po."orderId"
  WHERE o.status IN ('shipping', 'delivered', 'completed')
    AND po."vendorId" IS NOT NULL
  GROUP BY po."vendorId"
),
payments AS (
  SELECT vendor_id, SUM(amount) AS total
  FROM vendor_payments GROUP BY vendor_id
)
SELECT
  v.id, v.name,
  COALESCE(dp.total, 0) + COALESCE(sp.total, 0) AS total_purchases,
  COALESCE(pay.total, 0) AS total_payments,
  COALESCE(dp.total, 0) + COALESCE(sp.total, 0) - COALESCE(pay.total, 0) AS outstanding_balance
FROM vendors v
LEFT JOIN direct_purchases dp ON dp.vendor_id = v.id
LEFT JOIN site_purchases sp ON sp.vendor_id = v.id
LEFT JOIN payments pay ON pay.vendor_id = v.id
WHERE v."isActive" = true
ORDER BY outstanding_balance DESC;
```

### 5-6. API

| 엔드포인트 | Method | 설명 |
|-----------|--------|------|
| GET /api/admin/vendor-payments/summary | GET | 업체별 외상 현황 |
| GET /api/admin/vendor-payments/:vendorId/history | GET | 특정 업체 거래 내역 (매입+입금 시간순) |
| POST /api/admin/vendor-payments | POST | 입금 등록 |
| PUT /api/admin/vendor-payments/:id | PUT | 입금 수정 |
| DELETE /api/admin/vendor-payments/:id | DELETE | 입금 삭제 |
| GET /api/admin/vendor-payments/:vendorId/export | GET | 거래내역 엑셀 다운로드 |

---

## 6. 매출 현황 탭

### 6-1. 두 가지 매출 소스

1. **사이트 매출** — 기존 주문(orders) 데이터 자동 연동 (입력 불필요)
2. **직접 매출** — 사이트 외 납품 건을 수기 입력

### 6-2. 세금계산서 발행 기준 (중요)

> **세금계산서는 매월 말일 마감, 익월 10일 이내 발행**
> 따라서 회원별 월간 공급가액이 정확하게 집계되어야 합니다.

#### 핵심: 포인터 제외

```
회원 주문 결제 = 예치금(실제 입금액) + 포인터(사이트 보너스)

예치금: 회원이 실제 돈을 입금하여 충전 → 세금계산서 대상 ✅
포인터: 사이트에서 부여하는 보너스 → 세금계산서 대상 ❌

∴ 세금계산서 발행액 = 총 주문금액 - 포인터 사용액
```

**예시:**
| 회원 | 총 주문금액 | 예치금 결제 | 포인터 사용 | 세금계산서 발행액 |
|------|-----------|-----------|-----------|-----------------|
| 프레시마트 | 2,536,000 | 2,200,000 | 336,000 | **2,200,000원** |
| 과일나라 | 1,691,000 | 1,691,000 | 0 | **1,691,000원** |
| 탑셀01 | 1,057,000 | 900,000 | 157,000 | **900,000원** |

- 매출 기준일: 주문의 **배송 확정일** (status가 shipping으로 변경된 날짜)
- 월 마감 기준: 매월 1일 00:00 ~ 말일 23:59 (KST)
- 결제 내역은 settlement_history 테이블에서 예치금/포인터 차감 구분 가능

#### 세금계산서 금액 계산 SQL 참고

```sql
-- 회원별 월간 세금계산서 발행액
-- settlement_history에서 type='pointer'인 건을 제외하고 집계
SELECT
  m.id AS member_id,
  m.name AS member_name,
  m."businessName",
  m."businessNumber",
  SUM(sh.amount) AS total_order_amount,
  SUM(CASE WHEN sh.type = 'pointer' THEN sh.amount ELSE 0 END) AS pointer_used,
  SUM(CASE WHEN sh.type = 'deposit' THEN sh.amount ELSE 0 END) AS deposit_used,
  -- 세금계산서 발행액 = 예치금 결제분만
  SUM(CASE WHEN sh.type = 'deposit' THEN sh.amount ELSE 0 END) AS tax_invoice_amount
FROM settlement_history sh
JOIN orders o ON o.id = sh."orderId"
JOIN members m ON m.id = o."memberId"
WHERE o.status IN ('shipping', 'delivered', 'completed')
  AND sh."createdAt" >= '2026-02-01'
  AND sh."createdAt" < '2026-03-01'
GROUP BY m.id, m.name, m."businessName", m."businessNumber"
ORDER BY tax_invoice_amount DESC;
```

### 6-3. 화면 구성

```
┌──────────────────────────────────────────────────────────────┐
│  매출 현황                                                     │
│                                                                │
│  보기: [월별 매출 요약] [일별 상세] [직접 매출 관리]              │
│                                                                │
│  ═══════════════════════════════════════════════════════════   │
│  월별 매출 요약 (세금계산서 기준)                                 │
│  ═══════════════════════════════════════════════════════════   │
│                                                                │
│  월 선택: [2026년 2월 ▼]    마감 상태: 🟡 진행중 (2/11 현재)    │
│                             발행 기한: 2026-03-10까지           │
│                                                                │
│  ┌─ 매출 요약 ────────────────────────────────────────────┐    │
│  │  총 매출: 9,256,000원                                   │    │
│  │  ├─ 사이트 매출: 8,456,000원 (127건)                    │    │
│  │  └─ 직접 매출:    800,000원 (3건)                       │    │
│  │  공급가액: 8,414,545원  |  부가세: 841,455원             │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌─ 회원별 월간 세금계산서 발행액 ─────────────────────────┐      │
│  │                                                        │    │
│  │  회원명(업체명)  사업자번호   총주문액  포인터  발행대상액  공급가액   부가세│
│  ├────────────────────────────────────────────────────┤    │
│  │  프레시마트    123-45-67890  2,536K  -336K  2,200K  2,000,000  200,000│
│  │  과일나라     234-56-78901  1,691K     0   1,691K  1,537,273  153,727│
│  │  굿프루츠     345-67-89012  1,268K  -150K  1,118K  1,016,364  101,636│
│  │  탑셀01      456-78-90123  1,057K  -157K    900K    818,182   81,818│
│  │  과일천국     567-89-01234    892K     0     892K    810,909   81,091│
│  │  ...                                                  │    │
│  ├────────────────────────────────────────────────────┤    │
│  │  합계                      8,456K  -643K  7,813K  7,102,728  710,272│
│  └────────────────────────────────────────────────────┘    │
│                                                            │    │
│  ⚠️ 포인터 사용분은 세금계산서 발행 대상에서 제외됩니다.         │    │
│  📌 발행대상액 = 총 주문금액 - 포인터 사용액 (예치금 결제분만)   │    │
│  📌 공급가액 = 발행대상액 ÷ 1.1  |  부가세 = 발행대상액 - 공급가액│    │
│                                                            │    │
│  [📥 세금계산서용 엑셀 다운로드]                               │    │
│                                                                │
│  ℹ️ 공급가액 = 매출액 ÷ 1.1 (부가세 포함 가격 기준)              │
│  ℹ️ 회원 클릭 시 해당 회원의 월간 주문 상세 내역 표시             │
│                                                                │
│  ═══════════════════════════════════════════════════════════   │
│  일별 매출 상세                                                 │
│  ═══════════════════════════════════════════════════════════   │
│                                                                │
│  기간: [2026-02-01] ~ [2026-02-11]                             │
│  구분: [전체 ▼]  (전체 / 사이트 매출 / 직접 매출)                 │
│                                                                │
│  ┌─ 일별 매출 테이블 ──────────────────────────────────────┐    │
│  │ 날짜    사이트매출    직접매출    합계       일 변화       │    │
│  │ 2/11   768,000     300,000   1,068,000  ▲ +12.3%    │    │
│  │ 2/10   1,042,000   0         1,042,000  ▲ +18.3%    │    │
│  │ 2/9    680,000     500,000   1,180,000  ▲ +35.6%    │    │
│  │ ...                                                    │    │
│  ├──────────────────────────────────────────────────────┤    │
│  │ 합계   8,456,000   800,000   9,256,000               │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ═══════════════════════════════════════════════════════════   │
│  직접 매출 관리                                                 │
│  ═══════════════════════════════════════════════════════════   │
│                                                                │
│  [+ 직접 매출 등록]                                             │
│                                                                │
│  ┌─ 직접 매출 목록 ───────────────────────────────────────┐    │
│  │ □  날짜   거래처명     내용          금액     메모       │    │
│  │ □  2/11  대형마트A   사과 20박스 납품  300,000  정기납품  │    │
│  │ □  2/9   레스토랑B   딸기 10팩 납품   500,000  특별주문  │    │
│  │                                                        │    │
│  │  선택: [수정] [삭제]                                     │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

### 6-4. 회원 클릭 시 — 월간 주문 상세 (세금계산서 증빙용)

```
┌──────────────────────────────────────────────────────────┐
│  프레시마트 — 2026년 2월 공급 내역                          │
│                                                            │
│  사업자번호: 123-45-67890                                  │
│  대표자: 김영수  |  연락처: 010-1234-5678                   │
│                                                            │
│  ┌────────────────────────────────────────────────────────┐│
│  │ 날짜   주문번호          상품명     수량  단가    금액    ││
│  │ 2/3   ORD-20260203-01  사과10kg   5    40,000  200,000││
│  │ 2/3   ORD-20260203-01  배7.5kg    3    42,000  126,000││
│  │ 2/5   ORD-20260205-03  감귤5kg    4    28,000  112,000││
│  │ 2/7   ORD-20260207-02  딸기1kg    6    32,000  192,000││
│  │ ...                                                    ││
│  ├────────────────────────────────────────────────────────┤│
│  │ 합계                                      2,536,000원  ││
│  │ 공급가액: 2,305,455원  |  부가세: 230,545원              ││
│  └────────────────────────────────────────────────────────┘│
│                                                            │
│  [📥 공급 내역 엑셀 다운로드]  [🖨️ 인쇄]                    │
└──────────────────────────────────────────────────────────┘
```

### 6-5. 세금계산서용 엑셀 다운로드 형식

| 공급받는자 (상호) | 사업자번호 | 대표자 | 총주문액 | 포인터사용 | 발행대상액 | 공급가액 | 부가세 |
|-----------------|----------|--------|---------|----------|----------|---------|--------|
| 프레시마트 | 123-45-67890 | 김영수 | 2,536,000 | 336,000 | 2,200,000 | 2,000,000 | 200,000 |
| 과일나라 | 234-56-78901 | 박민수 | 1,691,000 | 0 | 1,691,000 | 1,537,273 | 153,727 |
| ... | | | | | | | |

- 포인터 사용액이 명확히 구분되어 세무 증빙 가능
- 홈택스 업로드 또는 세무사 전달용으로 활용

### 6-3. 직접 매출 등록 모달

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 매출일 | date | ✅ | 매출 날짜 (기본값: 오늘) |
| 거래처명 | text | ✅ | 납품처 이름 |
| 내용 | text | ✅ | 납품 내용 (상품명, 수량 등) |
| 금액 | number | ✅ | 매출 금액 (원) |
| 메모 | text | | 비고 |

### 6-6. 매출 기준
- **사이트 매출**: 주문 status = 'shipping' 이후 — 기존과 동일
- **직접 매출**: 등록 즉시 확정
- **총 매출 = 사이트 매출 + 직접 매출**
- **공급가액 = 매출액 ÷ 1.1** (부가세 포함 가격 기준)
- **부가세 = 매출액 - 공급가액**

### 6-7. DB 테이블: direct_sales

```sql
CREATE TABLE direct_sales (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sale_date DATE NOT NULL,
  client_name VARCHAR(200) NOT NULL,
  description TEXT NOT NULL,
  amount DECIMAL(14,0) NOT NULL,
  memo TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_direct_sales_date ON direct_sales(sale_date);
```

### 6-8. API

| 엔드포인트 | Method | 설명 |
|-----------|--------|------|
| GET /api/admin/accounting/sales | GET | 통합 매출 현황 (사이트+직접, 기간 필터) |
| GET /api/admin/accounting/sales/daily | GET | 일별 매출 집계 (사이트/직접 구분) |
| GET /api/admin/accounting/sales/monthly-by-member | GET | **회원별 월간 공급가액** (세금계산서용) |
| GET /api/admin/accounting/sales/member/:id/monthly-detail | GET | **특정 회원 월간 주문 상세** (증빙용) |
| GET /api/admin/accounting/sales/tax-invoice-export | GET | **세금계산서용 엑셀 다운로드** (홈택스 형식) |
| POST /api/admin/direct-sales | POST | 직접 매출 등록 |
| PUT /api/admin/direct-sales/:id | PUT | 직접 매출 수정 |
| DELETE /api/admin/direct-sales/:id | DELETE | 직접 매출 삭제 |
| GET /api/admin/accounting/sales/export | GET | 매출 현황 엑셀 다운로드 |

---

## 7. 손익 현황 탭

### 7-1. 계산 공식

```
총 매출 = 사이트 매출 + 직접 매출
총 매입 = 원물 + 반재료 + 부자재 + 기타
이익 = 총 매출 - 총 매입
마진율 = 이익 / 총 매출 × 100
```

### 7-2. 화면 구성

```
┌──────────────────────────────────────────────────────────────┐
│  손익 현황                                                     │
│                                                                │
│  기간: [2026-02-01] ~ [2026-02-11]                             │
│  보기: [일별 ▼]  (일별 / 주별 / 월별)                            │
│                                                                │
│  ┌─ 손익 요약 카드 ─────────────────────────────────────────┐  │
│  │                                                            │  │
│  │  📈 총 매출           📉 총 매입           💰 이익         │  │
│  │  9,256,000원         5,834,000원         3,422,000원      │  │
│  │  ├ 사이트 8,456K     ├ 원물   5,680K     마진율 37.0%     │  │
│  │  └ 직접    800K      ├ 반재료   450K     전기간 34.2%     │  │
│  │                      ├ 부자재 1,284K     ▲ +2.8%p        │  │
│  │                      └ 기타     820K                      │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                                │
│  ┌─ 매출/매입/이익 추이 차트 ──────────────────────────────┐    │
│  │  [매출 파란 라인 ── 매입 빨간 라인 ── 이익 초록 막대]      │    │
│  │  (이중 축: 좌측 금액, 우측 마진율 점선)                    │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌─ 일별 손익 테이블 ──────────────────────────────────────┐    │
│  │ 날짜    매출       매입       이익      마진율            │    │
│  │ 2/11  1,068,000  620,000   448,000   41.9%            │    │
│  │ 2/10  1,042,000  680,000   362,000   34.7%            │    │
│  │ 2/9   1,180,000  790,000   390,000   33.1%            │    │
│  │ ...                                                    │    │
│  ├──────────────────────────────────────────────────────┤    │
│  │ 합계  9,256,000  5,834,000 3,422,000  37.0%          │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌─ 매입 분류별 비중 ──────────────────────────────────────┐    │
│  │  [도넛 차트: 원물 69% / 부자재 16% / 기타 10% / 반재료 5%] │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  ┌─ 업체별 매입 현황 ──────────────────────────────────────┐    │
│  │ 업체명       분류      매입액     비중    외상잔액       │    │
│  │ 행복농장     원물     2,430,000  29.5%  1,230,000     │    │
│  │ 딸기마을     원물     1,944,000  23.6%    890,000     │    │
│  │ 제주감귤원   원물     1,306,000  15.9%    560,000     │    │
│  │ 프루츠팩     부자재   1,284,000  15.6%    320,000     │    │
│  │ CJ대한통운   기타       820,000  10.0%    180,000     │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
│  [📥 손익 보고서 다운로드 (엑셀)]                               │
└──────────────────────────────────────────────────────────────┘
```

### 7-3. API

| 엔드포인트 | Method | 설명 |
|-----------|--------|------|
| GET /api/admin/accounting/profit-loss | GET | 손익 요약 |
| GET /api/admin/accounting/profit-loss/trend | GET | 일별/주별/월별 손익 추이 |
| GET /api/admin/accounting/profit-loss/by-type | GET | 매입 분류별 비중 |
| GET /api/admin/accounting/profit-loss/by-vendor | GET | 업체별 매입 현황 |
| GET /api/admin/accounting/profit-loss/export | GET | 손익 보고서 엑셀 다운로드 |

---

## 8. DB 스키마 전체 요약

### 기존 테이블 확장 (필드 추가만)

```sql
-- vendors 테이블에 회계 관련 필드 추가 (기존 필드 수정 없음)
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS supply_type TEXT[] DEFAULT '{}';
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS business_number VARCHAR(20);
ALTER TABLE vendors ADD COLUMN IF NOT EXISTS address TEXT;
```

### 신규 테이블 3개

```sql
-- 1. 직접 매입 기록 (purchases) — 수기/엑셀 입력 건만 저장
--    사이트 외주거래는 여기에 저장하지 않고, 조회 시 기존 데이터에서 집계
CREATE TABLE purchases (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  purchase_date DATE NOT NULL,
  vendor_id INTEGER NOT NULL REFERENCES vendors(id),
  material_type VARCHAR(20) NOT NULL,      -- raw, semi, subsidiary, etc
  product_name VARCHAR(200) NOT NULL,
  quantity DECIMAL(10,2) NOT NULL,
  unit VARCHAR(20) NOT NULL,
  unit_price DECIMAL(12,0) NOT NULL,
  total_amount DECIMAL(14,0) NOT NULL,
  memo TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 2. 업체 입금 기록 (vendor_payments)
CREATE TABLE vendor_payments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  vendor_id INTEGER NOT NULL REFERENCES vendors(id),
  payment_date DATE NOT NULL,
  amount DECIMAL(14,0) NOT NULL,
  memo TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 3. 직접 매출 (direct_sales)
CREATE TABLE direct_sales (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  sale_date DATE NOT NULL,
  client_name VARCHAR(200) NOT NULL,
  description TEXT NOT NULL,
  amount DECIMAL(14,0) NOT NULL,
  memo TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 인덱스
CREATE INDEX idx_purchases_date ON purchases(purchase_date);
CREATE INDEX idx_purchases_vendor ON purchases(vendor_id);
CREATE INDEX idx_purchases_type ON purchases(material_type);
CREATE INDEX idx_vendor_payments_vendor ON vendor_payments(vendor_id);
CREATE INDEX idx_vendor_payments_date ON vendor_payments(payment_date);
CREATE INDEX idx_direct_sales_date ON direct_sales(sale_date);
```

### 기존 테이블 활용 (변경 없음)

| 테이블 | 회계장부에서의 용도 |
|--------|------------------|
| vendors | 공급업체 기본 정보 (회계 필드 추가만) |
| product_vendors | 상품별 매입가 (vendorPrice) → 사이트 매입 단가 기준 |
| pending_orders | 외주 배분 건 (vendorId) → 사이트 매입 자동 연동 |
| orders + order_items | 주문 확정 건 → 사이트 매출 + 사이트 매입 확정 시점 |
| members | 회원(셀러) 정보 → 세금계산서 발행 대상 |
| products / product_registrations | 상품 정보 + 카테고리 → 품목명 자동 완성 |

---

## 9. 향후 AI 통합 분석 연동

회계장부가 완성되면, AI가 답변 가능한 질문 예시:

| 질문 | 분석 |
|------|------|
| "이번 달 순이익 알려줘" | 총 매출(사이트+직접) - 총 매입 = 이익 |
| "마진율 높은 품목 TOP 5" | 품목별 매출-매입 마진율 비교 |
| "행복농장 거래 현황" | 매입 추이 + 단가 변화 + 외상 잔액 |
| "매입 단가 오른 품목" | 전월 대비 단가 변화율 |
| "부자재 비용이 매출 대비 몇 %?" | 부자재 매입 / 총 매출 비율 |
| "외상 잔액 많은 업체" | 업체별 외상 잔액 순위 |
| "납품(직접) 매출 현황" | 직접 매출만 필터 분석 |

---

## 10. 구현 순서

```
Step 1: vendors 테이블 + 공급업체 CRUD API + UI
Step 2: purchases 테이블 + 매입 등록(수기) + 목록 UI
Step 3: 매입 엑셀 업로드/다운로드/템플릿
Step 4: vendor_payments 테이블 + 매입 정산 (입금 등록 + 외상 관리) UI
Step 5: direct_sales 테이블 + 직접 매출 등록 + 매출 현황 탭
Step 6: 손익 현황 탭 (매출-매입 + 차트)
Step 7: 사이드바 메뉴에 회계장부 추가 + 라우팅
```

---

## 11. 기술 참고

- 차트: Recharts (기존 통계와 동일)
- 엑셀: SheetJS (xlsx 라이브러리)
- 날짜: 모든 날짜 KST 기준
- 금액: 원 단위, 천 단위 콤마 표시
- 외상 잔액: 별도 필드 없이 SUM(매입) - SUM(입금) 실시간 계산
- 반응형: 데스크톱 우선 (관리자용)

---

## 12. 담당자 편의성 UX 가이드라인 (중요)

> 회계장부는 **수기 작업이 매우 많은 영역**입니다.
> 담당자가 매일 반복적으로 사용하므로 **입력 속도와 편의성**이 최우선입니다.

### 12-1. 빠른 입력 원칙

#### 키보드 중심 입력
- **Tab 키**로 다음 필드 이동 (마우스 클릭 최소화)
- **Enter 키**로 등록 완료 또는 다음 행 추가
- 숫자 입력 필드에서 **자동 콤마 표시** (입력은 숫자만, 표시는 콤마 포함)
- 금액 필드: 수량 또는 단가 입력 시 **합계 자동 계산 + 실시간 갱신**

#### 자동 완성/최근 입력 활용
- **업체명**: 최근 거래한 업체 상단 표시 + 타이핑 시 자동 완성
- **품목명**: 해당 업체에서 이전에 매입한 품목 목록 자동 제안
  ```
  예: 행복농장 선택 → 품목명에 "사" 입력 → "사과 10kg (최근 단가: 14,000)" 자동 제안
  ```
- **단가**: 해당 업체+품목의 **최근 매입 단가 자동 입력** (수정 가능)
- **단위**: 해당 품목의 최근 사용 단위 자동 선택

#### 연속 입력 지원
- 매입 등록 후 **"등록하고 계속 입력"** 버튼 (모달 닫지 않고 다음 건 바로 입력)
- 같은 업체 연속 입력 시 업체 선택 유지
- 매입 등록 완료 시 **성공 토스트** 표시 (모달 유지)

### 12-2. 자주 쓰는 기능 빠른 접근

#### 오늘 할 일 한눈에
- 회계장부 진입 시 **오늘 요약 배너** 표시:
  ```
  📌 오늘 (2/11): 매입 5건 등록 | 미정산 업체 3개 (외상 합계 3,180,000원)
  📌 세금계산서: 1월분 발행 기한 2/10 ⚠️ 오늘까지!
  ```

#### 빠른 등록 버튼
- 모든 탭에서 접근 가능한 **플로팅 버튼** 또는 상단 **퀵 액션** 바:
  ```
  [+ 매입 등록]  [+ 입금 등록]  [+ 직접 매출]
  ```

### 12-3. 반복 작업 효율화

#### 매입 복사 기능
- 이전 매입 건 선택 → **[복사하여 등록]** → 날짜만 오늘로 변경, 나머지 동일
- 매일 같은 업체에서 비슷한 품목을 매입하는 경우 유용
  ```
  예: 어제 행복농장에서 사과 50박스 매입 → [복사] → 오늘 수량만 45로 수정 → 등록
  ```

#### 정기 매입 템플릿 (선택)
- 자주 반복되는 매입 패턴을 **템플릿으로 저장**
  ```
  예: "행복농장 기본 매입" = 사과10kg + 배7.5kg → 클릭 한 번에 품목 자동 채움
  ```

#### 엑셀 업로드 향상
- 업로드 시 **이전 업로드 파일 형식 기억** (열 순서가 달라도 매핑 가능)
- 오류 수정 후 **해당 행만 재업로드** 가능 (전체 다시 안 해도 됨)

### 12-4. 조회/확인 편의성

#### 즉시 검증 가능한 UI
- 매입 등록 후 **업체 외상 잔액 즉시 갱신** (화면 새로고침 불필요)
- 입금 등록 후 **잔액 변화 애니메이션** (이전 잔액 → 새 잔액)
- 손익 현황에서 **수치 클릭 시 해당 상세로 바로 이동** (드릴다운)

#### 월별 비교 한눈에
- 손익 현황에서 **이번 달 vs 지난 달** 나란히 비교 가능
- 매입 관리에서 **동일 품목 전월 단가 대비** 표시
  ```
  사과 10kg: 14,000원 (전월 13,200원 ▲ +6.1%)
  ```

### 12-5. 세금계산서 관련 편의 기능

#### 월 마감 상태 표시
- 매출 현황 탭 상단에 **마감 상태 배지**:
  - 🟢 마감 완료 (해당 월 데이터 확정)
  - 🟡 진행중 (아직 해당 월 미마감)
  - 🔴 발행 기한 임박 (익월 8일 이후 경고)

#### 세금계산서 발행 알림
- 매월 1일: "○월분 세금계산서 발행 대상 ○건, ○○○원" 알림
- 매월 8일: "세금계산서 발행 기한 2일 남았습니다" 경고

#### 회원별 공급 내역 인쇄/다운로드
- 세금계산서 증빙용 **회원별 월간 공급 내역서** 출력 가능
- 엑셀 다운로드: 홈택스 업로드 형식 또는 세무사 전달용

### 12-6. 실수 방지

#### 삭제 안전장치
- 삭제 시 **확인 대화상자** + 잔액 변화 안내
  ```
  "이 매입 건을 삭제하면 행복농장 외상 잔액이 1,230,000 → 530,000원으로 변경됩니다."
  ```
- 삭제 후 **5초간 [되돌리기] 버튼** 표시 (soft delete → 확정 삭제)

#### 입금액 검증
- 입금 등록 시 **외상 잔액 초과 경고**:
  ```
  ⚠️ 외상 잔액(1,230,000원)보다 큰 금액입니다. 선급금으로 처리됩니다.
  ```

> ⚠️ **중복 등록 경고는 적용하지 않음**
> 원재료 특성상 같은 업체+같은 품목+같은 금액의 매입이 매일 반복됩니다.
> 중복 경고를 넣으면 오히려 담당자 업무를 방해합니다.

---

## 13. 엑셀 스타일 인라인 입력 (핵심 UX)

> 담당자가 엑셀에 익숙하므로, 매입 관리의 **기본 입력 방식을 엑셀 시트 스타일**로 구현합니다.
> 별도 모달을 열지 않고, 테이블 안에서 바로 행을 추가하고 입력합니다.

### 13-1. 엑셀 스타일 매입 입력 화면

```
┌──────────────────────────────────────────────────────────────────┐
│  매입 관리                                                        │
│                                                                    │
│  기간: [2026-02-01] ~ [2026-02-11]                                │
│  분류: [전체 ▼]  업체: [전체 ▼]                                    │
│                                                                    │
│  [📤 엑셀 업로드] [📋 템플릿] [📥 다운로드]  모드: [조회] [✏️ 입력]  │
│                                                                    │
│  ┌─ [✏️ 입력 모드 ON] ──────────────────────────────────────────┐  │
│  │                                                                │  │
│  │  날짜       분류    업체명     품목명      수량  단위  단가    금액│  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  2/11      원물   행복농장   사과10kg    50   박스  14,000  700K │  │  ← 기존 데이터 (회색 배경)
│  │  2/11      원물   제주감귤   감귤5kg     30   박스  12,000  360K │  │
│  │  2/11      부자재  프루츠팩   포장박스     200  개   1,500   300K │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  [2/11]   [원물▼] [행복농장▼] [사과10kg]  [50] [박스▼] [14,000] 700K│  │  ← 새 입력 행 (파란 테두리)
│  │  [2/11]   [원물▼] [      ▼] [        ]  [  ] [   ▼] [      ]    │  │  ← 빈 입력 행
│  │  [2/11]   [원물▼] [      ▼] [        ]  [  ] [   ▼] [      ]    │  │
│  │                                                                │  │
│  │                                        [+ 행 추가]  [저장]     │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                    │
│  분류별 합계: 원물 5,680K | 반재료 450K | 부자재 1,284K | 기타 820K  │
│  총 매입액: 8,234,000원                                            │
└──────────────────────────────────────────────────────────────────┘
```

### 13-2. 입력 모드 동작 방식

#### 모드 전환
- **[조회] 모드**: 기존 매입 데이터 조회만 가능 (기본)
- **[✏️ 입력] 모드**: 테이블 하단에 빈 입력 행이 나타남 → 바로 입력 시작

#### 셀 이동 (엑셀 동일)
| 키 | 동작 |
|----|------|
| **Tab** | 다음 셀(오른쪽)로 이동 |
| **Shift+Tab** | 이전 셀(왼쪽)로 이동 |
| **Enter** | 현재 행 확정 + 다음 빈 행으로 이동 |
| **↑ ↓** | 위/아래 행 이동 |
| **Esc** | 현재 행 입력 취소 |

#### 자동 완성 동작
- **업체명 셀**: 타이핑 시 드롭다운 자동 완성 (최근 거래 업체 우선)
- **품목명 셀**: **재고관리에 등록된 원재료 자동 검색** + 이전 매입 이력 통합 제안
  ```
  품목명 셀 클릭 또는 타이핑 시 →
  ┌─────────────────────────────────────────┐
  │ 🔍 사과                                  │
  ├─────────────────────────────────────────┤
  │ 📦 재고 등록 원재료                       │
  │   사과 10kg (부사)      재고: 120박스     │
  │   사과 5kg (부사)       재고: 45박스      │
  │   사과 10kg (홍로)      재고: 30박스      │
  ├─────────────────────────────────────────┤
  │ 🕐 최근 매입 이력 (행복농장)              │
  │   사과 10kg (부사)      최근단가: 14,000  │
  │   사과 5kg (부사)       최근단가: 8,000   │
  ├─────────────────────────────────────────┤
  │ ✏️ 직접 입력: "사과"                      │
  └─────────────────────────────────────────┘
  ```
  - **상단**: 재고관리에 등록된 원재료에서 검색 (현재 재고수량 함께 표시)
  - **중단**: 선택된 업체의 이전 매입 품목 (최근 단가 표시)
  - **하단**: 목록에 없으면 직접 타이핑 가능
  - 재고 원재료 선택 시 → 품목명, 단위가 자동 채움
  - 매입 이력 선택 시 → 품목명, 단위, 최근 단가까지 자동 채움
- **단가 셀**: 업체+품목 선택 시 **최근 매입 단가 자동 채움** (수정 가능)
- **단위 셀**: 해당 품목의 최근 사용 단위 자동 선택
- **금액 셀**: 수량 × 단가 **자동 계산** (수정 불가, 자동 갱신)

#### 날짜 자동 채움
- 첫 번째 입력 행의 날짜 = 오늘 (기본)
- 다음 행부터 **이전 행의 날짜를 자동 복사** (같은 날짜 연속 입력 시 편리)
- 필요하면 날짜 직접 변경 가능

#### 분류 자동 채움
- 업체의 **공급 유형**에 따라 분류 자동 선택
  ```
  행복농장(원물 업체) 선택 → 분류 자동으로 "원물" 선택
  프루츠팩(부자재 업체) 선택 → 분류 자동으로 "부자재" 선택
  ```

### 13-3. 저장 방식

#### 행 단위 즉시 저장
- **Enter** 키 또는 다음 행으로 이동 시 해당 행 **자동 저장 (draft)**
- 하단 **[저장]** 버튼 클릭 시 **모든 draft 행을 한꺼번에 확정 저장**
- 저장 완료된 행은 **초록 체크 표시** 후 기존 데이터 영역으로 이동

#### 저장 전 검증
- 필수 필드 누락 시: 해당 셀 **빨간 테두리** + 포커스 이동
- 숫자 필드에 문자 입력 시: 입력 즉시 차단

### 13-4. 기존 데이터 인라인 수정

- 조회 모드에서도 행을 **더블 클릭** → 해당 행이 편집 모드로 전환
- 수정 후 **Enter** 또는 다른 행 클릭 시 자동 저장
- **Esc**로 수정 취소

### 13-5. 입금 등록도 동일한 방식

매입 정산 탭에서 업체별 거래 내역에도 인라인 입력 적용:

```
┌─ 행복농장 거래 내역 ──────────────────────────────────────┐
│                                                            │
│  날짜     구분      내용           금액       잔액          │
│  2/11    매입      사과10kg 50박스  +700,000  1,230,000    │
│  2/10    매입      배7.5kg 25박스   +500,000    530,000    │
│  2/9     입금      계좌이체         -800,000     30,000    │
│  ...                                                      │
├────────────────────────────────────────────────────────────┤
│  [2/11]  [입금]    [계좌이체      ] [        ] 원          │  ← 인라인 입금 입력
│                                                            │
│                    [저장]                                   │
└────────────────────────────────────────────────────────────┘
```

### 13-6. 엑셀 업로드와의 관계

- **소량 (1~10건)**: 엑셀 스타일 인라인 입력이 빠름
- **대량 (10건 이상)**: 엑셀 파일로 작성 후 업로드가 빠름
- 담당자가 상황에 따라 선택 가능
- 인라인 입력과 엑셀 업로드 결과는 동일하게 purchases 테이블에 저장
